export function StatusBadge({value,label}:{value:string;label?:string}){const style=({queued:'wait',succeeded:'good',running:'run',awaiting_approval:'wait',failed:'bad',cancelled:'bad',committed:'good',evaluating:'run',enabled:'good',acceptable:'wait',accepted:'good',applied:'good',rejected:'bad',proposed:'wait'}[value]??'muted');return <span className={`badge ${style}`}>{label??value}</span>}
